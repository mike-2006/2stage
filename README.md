Практическая работа — Вариант № 3

Тема: Анализ зависимостей пакетов Alpine Linux и визуализация графов

Цель работы

Изучить структуру пакетов Alpine Linux и на практике реализовать анализ зависимостей:
— чтение конфигурации из CSV;
— извлечение зависимостей из реального файла APKINDEX или тестового репозитория;
— построение графа зависимостей (DFS без рекурсии);
— анализ обратных зависимостей;
— визуализация графов с помощью Python и Mermaid.

Этап 1. Загрузка конфигурации

Чтение параметров из config.csv (ключ-значение).

Проверка корректности всех полей: package_name, repo_or_test_path, mode, version, max_depth.

Вывод параметров на экран при запуске.

Этап 2. Прямые зависимости

Поддержка двух режимов:

test — чтение простого графа из файла test_graph.txt;

real — автоматическая загрузка и разбор APKINDEX или APKINDEX.tar.gz с сервера Alpine.

Парсинг полей P: (имя), V: (версия), D: (зависимости).

Вывод всех прямых зависимостей для заданного пакета и версии.

Этап 3. Построение полного графа

Итеративный DFS (без рекурсии).

Учёт глубины max_depth.

Защита от циклов.

Параметр skip_substring для исключения лишних пакетов по подстроке.

Формирование списка смежности и порядка обхода.

Этап 4. Обратные зависимости

Автоматическое построение реверс-графа (кто зависит от кого).

Используется тот же DFS и ограничение глубины.

Печать порядка обхода и реверс-смежности.

Этап 5. Визуализация

Генерация текстового описания Mermaid → graph.mmd.

Создание HTML-файла graph.html (можно открыть в браузере).

Генерация полноценного изображения графа средствами Python (matplotlib):

graph.png — изображение графа;

graph.pdf — вариант для отчёта.

Автоматическое открытие HTML-диаграммы после выполнения.

Пример конфигурации (examples/config_real.csv)
package_name,busybox
repo_or_test_path,https://dl-cdn.alpinelinux.org/alpine/v3.20/main/x86_64/APKINDEX.tar.gz
mode,real
version,1.36.1-r15
max_depth,2
skip_substring,
visualize_mode,forward

Команды, выполняемые программой
🔸 Запуск
python main.py examples/config_real.csv

🔸 Основные этапы внутри программы

Этап 1: чтение CSV и вывод параметров

Этап 2: загрузка APKINDEX → извлечение прямых зависимостей

Этап 3: построение графа (DFS без рекурсии, max_depth)

Этап 4: построение обратного графа (reverse DFS)

Этап 5: визуализация

запись graph.mmd, graph.html, graph.png, graph.pdf

автоматическое открытие graph.html в браузере

Результаты работы

После успешного запуска в каталоге появляются:

graph.mmd     – текстовое описание Mermaid
graph.html    – HTML-визуализация
graph.png     – изображение графа
graph.pdf     – PDF-вариант для отчёта
